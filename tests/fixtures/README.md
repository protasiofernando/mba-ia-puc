# Fixtures públicas

`MANIFESTO_STAGE2_V6.json` contém somente metadados agregados, contagens e
hashes do insumo congelado. Não contém chaves nem texto de chamados. A fixture
permite testar a geração e a validação independente do pacote em um clone
limpo, sem depender da pasta privada `_hpc/`.
